{
  "name": "explorer-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ explorer-subgraph",
    "create-local": "graph create --node http://localhost:8020/ explorer-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ explorer-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 explorer-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "^0.29.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
